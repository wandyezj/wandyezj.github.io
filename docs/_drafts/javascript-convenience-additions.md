# javascript convenience additions

add iterable to set, so can take an array and add the entire thing to the set.

set.from method, like array.from

set operations
    - intersection
    - unions

IterableIterator built in map, forEach, filter, etc.. as done with array. This way these convenience methods can be applied to map and set.